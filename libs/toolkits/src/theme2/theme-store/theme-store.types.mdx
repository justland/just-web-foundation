import { Meta } from '@storybook/addon-docs/blocks'
import { MDXBadges } from 'storybook-addon-tag-badges/manager-helpers'
import ThemeStoreTypesStoriesMeta from './theme-store.types.stories'

<Meta title="theme2/theme-store/ThemeStore" />

# ThemeStore <MDXBadges of={ThemeStoreTypesStoriesMeta} />

`ThemeStore` in an interface that defines the methods to read/write/subscribe to a theme store.
Data flow participation is inferred based on which methods are implemented.
