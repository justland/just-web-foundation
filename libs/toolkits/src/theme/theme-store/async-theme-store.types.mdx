import { Meta } from '@storybook/addon-docs/blocks'
import { MDXBadges } from 'storybook-addon-tag-badges/manager-helpers'
import AsyncThemeStoreTypesStoriesMeta from './async-theme-store.types.stories'

<Meta title="theme/theme-store/AsyncThemeStore" />

# AsyncThemeStore <MDXBadges of={AsyncThemeStoreTypesStoriesMeta} />

`AsyncThemeStore` is an interface that defines async/sync methods to read, write, and subscribe to theme state.
All methods are optional.
Data flow participation is inferred from which methods you implement.

Use for remote persistence, polling, or WebSocket-based sync.

## Methods

- **read** – Can return `Promise<ThemeEntry | undefined | null>` for async sources.
- **write** – Can return `Promise<void>` for async persistence.
- **subscribe** – Same signature as sync; observes external changes.

## See also

- [ThemeStore](?path=/story/theme-theme-store-themestore--source) – Sync counterpart.
