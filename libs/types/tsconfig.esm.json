{
  "extends": "@tools/typescript/tsconfig.react.json",
  "compilerOptions": {
    // currently this package publishes the source TypeScript files.
    // We set the `outDir` to esm just to avoid confusion.
    "outDir": "esm",
    "rootDir": "src"
  },
  "include": ["src", "types"]
}
