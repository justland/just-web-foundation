---
"@just-web/toolkits": minor
---

Add optional `parse` option to `cookieThemeStore`, `localStorageThemeStore`, and `sessionStorageThemeStore` for custom parsing of stored theme strings. Export `ParseStoredTheme` type from theme entry point for typing custom parsers.
