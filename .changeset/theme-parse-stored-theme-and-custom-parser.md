---
"@just-web/toolkits": minor
---

Export `parseStoredTheme` and `ParseStoredTheme` from theme entry point. Add optional `parse` option to `cookieThemeStore`, `localStorageThemeStore`, and `sessionStorageThemeStore` for custom parsing of stored theme strings.
