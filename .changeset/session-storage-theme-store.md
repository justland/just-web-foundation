---
"@just-web/toolkits": minor
---

Add `sessionStorageThemeStore(storageKey)` for a theme store backed by sessionStorage for a fixed key. Refactor `getThemeFromSessionStorage`, `setThemeToSessionStorage`, and `observeThemeFromSessionStorage` to use the store internally. Export `SessionStorageThemeStore` and `SessionStorageThemeStoreOptions` types.
