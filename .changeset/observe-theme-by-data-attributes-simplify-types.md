---
"@just-web/toolkits": patch
---

Simplify `observeThemeByDataAttributes` options type: single signature with `allowCustom?: true` and `defaultTheme?: string`. Removes overload duplication; behavior unchanged.
