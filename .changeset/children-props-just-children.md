---
"@just-web/toolkits": minor
---

Add `JustChildrenProps`, `JustChildren`, `JustChildrenFnProps` types and `resolveChildren` utility for render-props-aware `children` handling, mirroring the existing style and class-name APIs.
