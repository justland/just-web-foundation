---
"@just-web/toolkits": minor
---

Export `parseClassName`, `stringifyClassName`, `readClassName`, `writeClassName`, and `subscribeClassName` for className theme handling. `classNameThemeStore` now delegates to these functions and supports optional `parse` and `stringify` options for customization.
