---
"@just-web/css": minor
---

Add new `Properties` interface and `defineProperties` function with improved type parameters for CSS properties. The old `CSSProperties` interface and `defineCSSProperties` function are now deprecated but remain available for backward compatibility.

Re-export other types from `csstype` package.